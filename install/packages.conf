#!/bin/bash

# -----------------------------------------------------------------------------
# Package definitions - categorized arrays for Homebrew
# -----------------------------------------------------------------------------

# Core CLI tools
CLI_TOOLS=(
  neovim
  tmux
  starship
  lazygit
  bat
  ripgrep
  fd
  fzf
  git-delta
  zoxide
  stow
  jq
  worktrunk
  dlvhdr/formulae/diffnav
  anomalyco/tap/opencode
  pnpm
)

# Development tools
DEV_TOOLS=(
  fnm
)

# macOS cask applications
CASK_APPS=(
  ghostty
  raycast
  1password
  spotify
  linear-linear
)

# Fonts (requires: brew tap homebrew/cask-fonts, or just works on newer brew)
FONTS=(
  font-jetbrains-mono-nerd-font
)
